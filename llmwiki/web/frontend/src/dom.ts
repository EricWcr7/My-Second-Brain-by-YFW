// Module scripts are deferred, so the DOM is parsed by the time these run.

export const content = document.getElementById("content") as HTMLElement;
export const pagesEl = document.getElementById("pages") as HTMLElement;
export const filterEl = document.getElementById("filter") as HTMLInputElement;
